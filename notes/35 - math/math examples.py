import math

#Convert a Degree Value to Radians
degrees = 90
radians = math.radians(degrees)
print(f'{degrees} deg is equivalent to {radians} rad')

#Convert a Radians Value to Degrees
radians = 3.14
degrees = math.degrees(radians)
print(f'{radians} rad is equivalent to {degrees} deg')